const express = require("express");
const app = express();

app.get();
app.post();
app.put();
app.delete();

//route ->
// app.get(path, routeHandler)

// routeHandler ->
// (req,res) => {res.send('Hello World')}

// app.listen()
